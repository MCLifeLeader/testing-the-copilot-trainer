# Create Blazor UI and API endpoints for contact search and management

- Implement search by display name, username, or email with fuzzy matching.
- Allow sending, accepting, rejecting contact requests.
- Display pending, accepted, and blocked contacts in UI.
- Add backend endpoints for managing contact relationships.
